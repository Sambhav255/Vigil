import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Hide Next.js dev corner indicator (red “N” / build badge) in development — H4 */
  devIndicators: false,
};

export default nextConfig;
